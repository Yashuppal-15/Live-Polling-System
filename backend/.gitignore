# Node modules (defensive; root .gitignore should already ignore this)
node_modules

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env/config
.env
.env.local

# Build / dist output
dist
build
coverage
